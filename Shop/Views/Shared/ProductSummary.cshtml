@model Product

<div class="card card-outline-primary m-1">
    <div class="row">
        <a class="row align-content-center text-decoration-none" style="color:black;" asp-action="Product" asp-controller="Product" asp-route-id="@Model.Id">
            <div class="col-sm-12 col-md-2 col-lg-2">
                <img class="card-img"
                     src="https://catherineasquithgallery.com/uploads/posts/2021-03/1614553939_1-p-kartinka-yabloka-na-belom-fone-1.jpg"
                     alt="No image" />
            </div>
            <div class="col-sm-12 col-md-8 col-lg-8 align-content-center text-lg-start text-center" >
                <div class="text-sm-center text-md-start text-lg-start p-1">
                    <h4>
                        @Model.Name
                    </h4>
                    <h5>@Model.Category</h5>
                    <span>@Model.Description</span>
                </div>
            </div>
            <div class="col-sm-12 col-md-2 col-lg-2">
                <h3 class="btn active rounded-pill @(Model.isAvailable ? "bg-primary" : "bg-danger")" style="float:right">
                    <span>@(Model.isAvailable ? @Model.Price.ToString("C") : "Unavailable")</span>
                </h3>
                @* <form id="@Model.Id" asp-action="AddToCart" asp-controller="Cart" method="post">
                <input type="hidden" asp-for="Id" />
                <input type="hidden" name="ReturnUrl" value="@ViewContext.HttpContext.Request.PathAndQuery()" />
                <span class="card-text p-1">
                    <button type="submit" class="btn btn-success btn-sm pull-right" style="float:right">
                        Add To Cart
                    </button>
                </span>
            </form> *@
            </div>
        </a>
        
    </div>
</div>

@*<div class="card h-100">
    <img class="card-img card-img-top" src="~/images/1614553939_1-p-kartinka-yabloka-na-belom-fone-1.jpg" />
    <div class="card-body text-center">
    <h5 class="card-title">@Model.Name</h5>
    <a href="#">
    <span class="badge badge-pill @(Model.isAvailable ? "badge-primary" : "badge-danger")">
    <small>@(Model.isAvailable ? @Model.Price.ToString("C") : "Unavailable")</small>
    </span>
    </a>
    </div>
    </div>*@
